from setuptools import setup

setup(
    name="sales_to_forecast",
    version="0.0.1",
    author="Kirby Team",
    license="MIT",
    packages=["sales_to_forecast"],
    install_requires=["kirby"],
)
