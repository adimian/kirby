from distutils.core import setup

setup(
    name="parse_surplus",
    version="0.0.1",
    author="Kirby Team",
    license="MIT",
    packages=["parse_surplus"],
    install_requires=["kirby", "example_utils"],
)
